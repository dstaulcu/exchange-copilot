# Exchange MCP Server Dependencies

# MCP Protocol
mcp>=1.0.0

# Vector Database (use 0.4.x for Windows stability)
chromadb>=0.4.24,<0.5.0

# Data validation
pydantic>=2.0.0

# NumPy (use 1.x for onnxruntime compatibility on Windows)
numpy>=1.24.0,<2.0.0

# LangChain for conversational AI
langchain>=0.3.0
langchain-ollama>=0.2.0
langchain-core>=0.3.0

# Note: sentence-transformers is optional. ChromaDB's built-in ONNX embeddings work.
# If you need custom embeddings and PyTorch works: pip install sentence-transformers
